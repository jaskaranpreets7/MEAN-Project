let UserDetails = function (firstName, lastName , userName, password, email){
    this.firstName = firstName,
    this.lastName = lastName,
    this.userName = userName,
    this.email = email,
    this.password = password
};

module.export.UserDetails;